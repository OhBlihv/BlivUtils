Voting
	If player is offline/slightly misspelt, then try to fix it.

Currently Online Staff, with AFK Status.

(LOW PRIORITY) - Add Scheduled ASync events instead of clogging up the main thread.

(LOW PRIORITY) - Add Utilities class with translateColours(String) and other useful things that I keep repeating
						- b.printError and b.printSuccess?

(LOW PRIORITY) - One time nickname change on join -- No colours.

(LOWER PRIORITY) - Longer duration AFK kick for Admins -- > 15 minutes. -- Stops them idling for so long.